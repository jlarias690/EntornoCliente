do{
var num = prompt("Escribe un numero mayor o igual a cero");
}while(isNaN(num) || num<=0)

  if (num >= 0) {
    var factorial = (num) => {
     Number(total) = 1;
     
      for (i = 0; i < num; i++) {
      total = total * i;}
      return total;
    }
};

document.write(factorial(num));
